
# MacOS folder metadata, not relevant to codebase
.DS_Store


# contains installed node modules and dependencies, should be installed with `npm install` rather than from version control
# Deno doesn't use node_modules, but we'll ignore it just in case we use it
# for something parallel to Deno
node_modules

# .env files contain sensitive information like API keys, database passwords, etc. Should be kept private and not tracked in version control for security reasons.
.env

# secrets.js files likely contain sensitive information, similar to .env. It should be kept out of version control to avoid exposing confidential data.
secrets.js

# Commonly a directory for built or compiled files, like the output of a build process. These files are generated from source and do not need to be tracked.
out

# created by text_to_speech.js, doesn't need to be tracked
out.mp3

# ..
sabines_rpg_battle.js
simons_rpg_battle.js